# Fog of War Architecture Specification

## Overview
This document specifies the required architecture for the Fog of War system in the game. It defines how visibility is calculated and rendered efficiently, supporting exploration and line-of-sight without relying on full-map recalculations every tick.

## Visibility States
The system must support the following strict visibility states for any given area:
1. **UNEXPLORED:** Black, totally hidden area.
2. **EXPLORED:** Area that was previously seen but is currently not in active line-of-sight. Usually rendered with a darkening overlay ("shroud").
3. **VISIBLE:** Area currently within the active line-of-sight of allied units. Fully visible.

## Performance and Partitioning Constraint
Fog of War updates must avoid full-map recalculations every game tick.
- **Chunk/Grid Partitioning:** The map must be divided into a grid or chunks. Visibility logic should only process updates for cells/chunks affected by moving units.

## Rendering Architecture: CanvasItem and SubViewport Masking
The visual representation of Fog of War leverages Godot's 2D rendering features.

### Required Approach
1. **SubViewport as a Mask Generator:**
   - A dedicated `SubViewport` should be used to render the "visibility map."
   - Active units draw their sight radiuses (using simple shapes, e.g., circles) onto this `SubViewport`.
   - This viewport acts as a dynamic mask representing the `VISIBLE` state.

2. **CanvasItem Material / Shaders:**
   - The game world (or a screen-covering overlay) applies a custom shader via `CanvasItem` materials.
   - The shader takes the texture generated by the `SubViewport` as an input uniform.
   - Using this mask texture, the shader determines per-pixel whether to render the map fully, apply an `EXPLORED` tint, or render pure black for `UNEXPLORED`.

### Explicit Directives
- **Do not** use large numbers of individual `Sprite2D` or `Light2D` nodes spanning the entire map to fake fog, as this scales poorly.
- Any UI or rendering mask modifications related to the Fog of War **must** be explicitly scoped within this `CanvasItem` and `SubViewport` masking architecture to maintain performance and maintainability.
